    //puts("Enter key pass (enter positive number only):");
    //int check = scanf("%d", &key);
    //int c = getchar();
/*
    if(c != '\n' && c != EOF)
    {
        invalid_args();
    }
    
    if (check != 1 || key < 0)
    {
        invalid_args();
    }
 
    if(key > 26) 
    {
        while (key >= 26)
        {
            key = key % 26;
        }
    }
*/
